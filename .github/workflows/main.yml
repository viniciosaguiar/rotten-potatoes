# This is a basic workflow to help you get started with Actions

name: CI-CD

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  CI:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - uses: actions/checkout@v2
    - name: docker login
      env:
        DOCKER_USER: ${{secrets.DOCKERHUB_USER}}
        DOCKER_PASSWORD: ${{secrets.DOCKERHUB_PWD}}
      run: |
        docker login -u ${{secrets.DOCKERHUB_USER}} -p ${{secrets.DOCKERHUB_PWD}}
    - name: Build the Docker image latest
      run: docker build ./src --tag viniciosaguiar/rotten-potatoes:latest
    - name: Build the Docker image version number
      run: docker build ./src --tag viniciosaguiar/rotten-potatoes:${{github.run_number}}
    - name: Docker Push latest
      run: docker push viniciosaguiar/rotten-potatoes:v5
    - name: Docker Push version number
      run: docker push viniciosaguiar/rotten-potatoes:${{github.run_number}}
      

        
          
          

